// Problem 3. Rectangle area
// Write an expression that calculates rectangle’s area by given width and height.

var width = 2.5;
var height = 3;

console.log(rectArea(width, height));

function rectArea(width, height) {
	return width*height;
}